// Auth API — será implementado na Fase 2
// Este arquivo é um placeholder para manter a estrutura de rotas.
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'Auth não configurado nesta fase.' }, { status: 501 });
}

export async function POST() {
  return NextResponse.json({ message: 'Auth não configurado nesta fase.' }, { status: 501 });
}
